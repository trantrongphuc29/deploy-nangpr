{
  "name": "deploy-nangpr",
  "version": "1.0.0",
  "private": true,
  "description": "Nang PR Coffee - monorepo deploy root (frontend CRA + backend Express)",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cloudinary": "^2.10.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "mysql2": "^3.20.0"
  }
}
